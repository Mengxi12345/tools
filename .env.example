# CAAT 阿里云生产环境配置
# 使用方式: cp .env.example .env 然后修改 .env 中的值
# 注意: .env 包含敏感信息，请勿提交到版本库

# ========== 阿里云镜像（必填） ==========
# 推送镜像后，从 .docker-images-aliyun.txt 或阿里云控制台获取
BACKEND_IMAGE=crpi-anqb8q8dr9wohaz8.cn-beijing.personal.cr.aliyuncs.com/mengxi666/mengxi666-backend:1.0.0
FRONTEND_IMAGE=crpi-anqb8q8dr9wohaz8.cn-beijing.personal.cr.aliyuncs.com/mengxi666/mengxi666-frontend:1.0.0

# 若使用 VPC 内网拉取（更快、不耗公网流量），可改为：
# BACKEND_IMAGE=crpi-anqb8q8dr9wohaz8-vpc.cn-beijing.personal.cr.aliyuncs.com/mengxi666/mengxi666-backend:1.0.0
# FRONTEND_IMAGE=crpi-anqb8q8dr9wohaz8-vpc.cn-beijing.personal.cr.aliyuncs.com/mengxi666/mengxi666-frontend:1.0.0

# ========== 数据库（必填） ==========
DB_USER=caat_user
DB_PASSWORD=请修改为强密码
DB_NAME=caat_db
DB_PORT=5432

# ========== Redis ==========
REDIS_PORT=6379

# ========== RabbitMQ ==========
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=admin

# ========== JWT 密钥（必填，生产环境务必修改） ==========
# 至少 256 位，建议使用 openssl rand -base64 48 生成
JWT_SECRET=请修改为至少32字符的随机字符串

# ========== 服务端口 ==========
BACKEND_PORT=8080
FRONTEND_PORT=80
